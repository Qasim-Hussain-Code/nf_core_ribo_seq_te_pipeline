## Execution reports

Nextflow's own execution report, timeline and process DAG for each run.
These are provenance records of a particular execution rather than
scientific output, and are excluded here; they are written automatically,
under a timestamped filename, every time the pipeline runs.

## Strand-specific coverage tracks

Per-library, per-strand wiggle (`.wig`) coverage tracks written by
RiboTricer, useful for visual inspection in a genome browser but not
otherwise required by any downstream step. Excluded here; regenerated by
re-running the pipeline.
